@model IEnumerable<LibroDiarioMayorWebApp.Models.Catalogo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreCuenta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TipoCuenta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IncluidaEn)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreCuenta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TipoCuenta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IncluidaEn)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NumeroCuenta">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NumeroCuenta">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NumeroCuenta">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
