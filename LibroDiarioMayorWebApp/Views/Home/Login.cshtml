@{
    ViewData["Title"] = "Iniciar Sesión";
    Layout = "_LayoutVacio";
}
<div class="row mt-5 justify-content-center">
    <div class="col-sm-4">
        <div class="card">
            <div class="card-header">
                <h3 style="font-weight:bold" class="mb-2">Iniciar Sesión</h3>
            </div>
            <div class="card-body">
                <form asp-controller = "Home" asp-action="Iniciar">
                    <div class="d-grid mb-2">
                    <label class="form-label" for="NombreUsuario">Nombre de Usuario</label>
                    <input class="form-control mb-3" type="text" name="NombreUsuario" value=""/>
                    </div>

                    <div class="d-grid mb-xxl-5">
                    <label class="form-label" for="Clave">Contraseña</label>
                    <input class="form-control mb-3 gap-xxl-3" type="password" name="Clave" value=""/>
                    </div>

                    <div class="d-grid mb-2">
                        <input class="btn btn-primary" type="submit" value="Ingresar"/>
                    </div>

                    @if (TempData["Mensaje01"] != null)
                    {
                        <div class="alert alert-danger" role="alert">
                            @TempData["Mensaje01"]
                        </div>
                    }
                    @if (TempData["Mensaje02"] != null)
                    {
                        <div class="alert alert-success" role="alert">
                            @TempData["Mensaje02"]
                        </div>
                    }
                </form>
            </div>
        </div>
    </div>
</div>
